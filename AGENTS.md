# AGENTS.md — Dear 仓库协作规范

你正在为 `XuShan0425/Dear` 仓库工作。

## 产品定位
Dear 是一个面向休学 / 迷茫 / 焦虑青少年的“平行时空同龄挚友”AI 对话产品。
它的核心不是普通聊天，而是：
- 极度安全
- 零爹味
- 零评判
- 通过感官细节、生活碎片、镜像共情来陪伴用户

当前产品方向：
1. 稳定前端与后端
2. 建立后台管理系统，便于主理人管理用户与风险
3. 将主 agent 做成可独立调试、可版本化、可定制的核心层
4. 为未来“数字挚友定制”商业化做准备

## 最高优先级
1. 主 agent 核心优先于普通页面功能
2. 模块边界清晰优先于快速堆代码
3. 可审查、可维护、可回滚优先于一次性大改
4. PR 可读性优先于炫技式实现

## 仓库工作原则
- 一次任务只处理一个主题
- 不要把多个方向混在一个 PR：前端 / 后端 / 后台 / agent core 要尽量分开
- 不要把新逻辑继续堆进单文件
- 优先拆分模块、常量、配置、工具函数、服务层
- 非必要不要重写已工作的逻辑，优先做最小必要修改
- 涉及接口行为变化时，必须说明影响范围与兼容性

## 关于主 agent 的强制规则
- 主 agent 是 Dear 的核心资产，不能被随意弱化、稀释、改味
- 不得把主 agent 写成“心理医生”“长辈”“导师”“客服”
- 不得把回复风格改成长篇建议流、列表说教流、标准 AI 口吻
- 需要改主 agent 时，优先做“结构优化、配置抽离、版本化、可测试化”
- 除非任务明确要求，不要随意改写核心人格文本
- 如果需要移动 prompt，请做“抽离与组织”，不要做语义重写
- 若主 agent 相关改动会影响产品气质，必须在最终说明中明确指出

## 模块化要求
新增代码或重构代码时，优先按照职责拆分：
- 路由层：只处理请求/响应，不承载复杂业务
- 服务层：处理业务逻辑
- agent 层：处理人格、场景、安全、定制逻辑
- 数据层：处理数据库与存储
- 配置层：处理环境变量、常量、模型参数
- UI 层：只负责展示与交互，不内嵌复杂业务规则

## 文件头说明要求
每个新增文件，或被大幅重构的文件，必须在头部写语言对应的注释，说明：
1. 这个文件实现什么功能
2. 它负责什么
3. 它不负责什么
4. 对外暴露什么
5. 依赖哪些关键模块

## 注释要求
- 复杂逻辑必须写注释
- 安全相关逻辑必须写注释
- 认证、会话、权限、风控、限流相关逻辑必须写注释
- 注释重点解释“为什么这样实现”，不是简单复述代码字面意思

## PR 与最终说明要求
不要在执行过程中不断输出状态播报。
在最终回复和 PR 描述中，必须清楚写出：
1. 本次目标
2. 修改了哪些文件
3. 每个文件为什么改
4. 核心实现思路
5. 风险点 / 兼容性影响
6. 验证步骤
7. 后续建议

如果涉及主 agent，必须额外写：
- 影响的是哪一层：人格层 / 安全层 / 场景层 / 定制层 / 评测层 / 版本层
- 预期会影响哪类对话
- 需要回归测试哪些典型句子

## 代码风格要求
- 可读性优先
- 命名清楚优先
- 小函数优先于超长函数
- 单一职责优先
- 避免“魔法字符串”散落
- 公共常量和重复逻辑要抽离
- 不要为了“看起来高级”而牺牲维护性

## 默认交付标准
只有同时满足以下条件，任务才算完成：
- 功能实现符合目标
- 代码结构清晰
- 注释足够让主理人看懂
- 最终说明足够让主理人审 PR
- 验证步骤清晰
